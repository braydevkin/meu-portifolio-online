import { TechPercentageDirective } from './tech-percentage.directive';

describe('TechPercentageDirective', () => {
  it('should create an instance', () => {
    const directive = new TechPercentageDirective();
    expect(directive).toBeTruthy();
  });
});
